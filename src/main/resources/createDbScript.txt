DROP TABLE IF EXISTS SavedGameGomoku;
CREATE OR REPLACE TABLE SavedGameGomoku (
ID INT PRIMARY KEY AUTO_INCREMENT,
NAME VARCHAR(20) NOT NULL UNIQUE,
TABLEDESCRIPTION VARCHAR(400) NOT NULL,
IS_X_TURN BOOLEAN NOT NULL,
IS_HUMAN_PLAYS_X BOOLEAN NOT NULL);

INSERT INTO SavedGameGomoku(NAME, TABLEDESCRIPTION, IS_X_TURN, IS_HUMAN_PLAYS_X)
VALUES
('Game1', '        XO       ', 1, 1),
('Game2', '   OX            ', 0, 1),
('Game3', '               XO  ', 0, 0);